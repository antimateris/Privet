package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Comfortable White Theme Palette (Enak di pandang & nyaman)
val SoftComfortBackground = Color(0xFFF8FAFC) // Soft soothing warm off-white (anti-glare)
val CrispCardWhite = Color(0xFFFFFFFF)        // Clean white for cards & elevated elements
val SoftSurfaceVariant = Color(0xFFF1F5F9)    // Gentle slate tint for chips and inner containers

val DeepSlateOnSurface = Color(0xFF1E293B)    // Charcoal navy for primary text (comfortable contrast)
val MutedSlateOnVariant = Color(0xFF64748B)   // Balanced slate for subtitles & captions
val SoftBorderOutline = Color(0xFFE2E8F0)     // Delicate soft border for cards
val SoftDivider = Color(0xFFEEF2F6)

// Calming, eye-friendly brand accents
val OceanPrimary = Color(0xFF0284C7)          // Clean sky/ocean blue
val OceanPrimaryContainer = Color(0xFFE0F2FE) // Soft sky pastel tint
val OnOceanPrimaryContainer = Color(0xFF0369A1)

val OceanSecondary = Color(0xFF0D9488)        // Calming teal sage
val OceanSecondaryContainer = Color(0xFFCCFBF1)
val OnOceanSecondaryContainer = Color(0xFF115E59)

val OceanTertiary = Color(0xFFD97706)         // Warm friendly amber
val OceanTertiaryContainer = Color(0xFFFEF3C7)
val OnOceanTertiaryContainer = Color(0xFF92400E)

val SuccessGreen = Color(0xFF16A34A)
val SuccessGreenContainer = Color(0xFFDCFCE7)
val OnSuccessGreen = Color(0xFF14532D)

val MoneyBlue = Color(0xFF0284C7)
val MoneyBlueContainer = Color(0xFFE0F2FE)

val AmberShare = Color(0xFFD97706)
val AmberShareContainer = Color(0xFFFEF3C7)

// Dark Theme fallback if ever needed
val OceanPrimaryDark = Color(0xFF81D4FA)
val OceanSecondaryDark = Color(0xFF80CBC4)
val OceanTertiaryDark = Color(0xFFFFB74D)
val DarkBackground = Color(0xFF0F172A)
val DarkSurface = Color(0xFF1E293B)
val DarkSurfaceVariant = Color(0xFF334155)
